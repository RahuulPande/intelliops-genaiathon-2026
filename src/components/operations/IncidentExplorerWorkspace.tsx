'use client';

export default function IncidentExplorerWorkspace(props: Record<string, unknown>) {
  return (
    <div className="p-8 text-center text-gray-500 dark:text-gray-400">
      <p className="text-lg font-semibold">IncidentExplorerWorkspace</p>
      <p className="text-sm mt-2">Available in Development Mode</p>
    </div>
  );
}
